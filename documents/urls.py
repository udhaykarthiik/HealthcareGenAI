from django.urls import path
from . import views

# URL patterns for document-related pages
urlpatterns = [
    # We'll add our views here in Phase 4
    # For now, just an empty list to prevent the error
]
